import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  day: {
    padding: '1rem',
    backgroundColor: 'white',
    '&:hover': {
      cursor: 'pointer',
    },
  },
}));
