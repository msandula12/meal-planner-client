import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  drawerHeader: {
    padding: '1rem',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
}));
