export enum MealType {
  BREAKFAST = 'breakfast',
  LUNCH = 'lunch',
  DINNER = 'dinner',
}

export enum Routes {
  DEMO = '/schedule-demo',
  HOME = '/',
  NOT_FOUND = '*',
  SCHEDULE = '/schedule',
}
